def parallelized_sum_values_in_array(array: list[int]) -> int:
    """
    Parallelizing a sum over an array

    Args
    ----
    array: list[int]
        Input array which should be summed over

    Returns
    -------
    int
        The result of the summation
    """
    ...


def sum_values_in_array(array: list[int]) -> int:
    """
    Summation over an array

    Args
    ----
    array: list[int]
        Input array

    Returns
    -------
    int
        Result of summation
    """
    ...